import 'package:flutter/material.dart';

class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Настройки пользователя\n(будут добавлены позже)', textAlign: TextAlign.center),
    );
  }
}
